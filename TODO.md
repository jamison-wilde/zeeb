# TODO

* Add bulk IMDB rating updating in the filename, and potentially bulk renaming in general that doesn't require intervention and may be renaming already named files (with tt# in them).
* Rename companion files (.jpg, .nfo, .url/.webloc) that share the movie's base name during rename, so existing posters/NFOs don't become orphans.
